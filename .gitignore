# Python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/
venv/
.venv/
env/

# OS
.DS_Store
Thumbs.db

# Streamlit
.streamlit/secrets.toml

# Reports (kept locally, excluded from the GitHub repo)
reports/

# Local backups / unused source assets (kept on disk, not in the repo)
app_original_backup.py
assets/*_original.png
assets/home hero.png

# Large model artifacts are kept (needed by the app), but ignore these if you
# prefer to regenerate them with train_models.py:
# models/*.pkl
# models/word2vec.model
